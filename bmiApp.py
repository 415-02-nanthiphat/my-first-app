import streamlit as str

# ส่วนที่ 1 หัวข้อหน้าเว็บ (Title สีแดง)
str.markdown("# :red[* คำนวณค่าดัชนีมวลกาย BMI *]")
str.write("กรอกข้อมูลน้ำหนักและส่วนสูงของคุณ เพื่อเช็คสุขภาพเบื้องต้น")

# ส่วนที่ 2 สร้างช่องรับค่าน้ำหนัก และ ส่วนสูง
weight = str.number_input("กรอกน้ำหนักของคุณ (กิโลกรัม):", min_value=1.0)
height_cm = str.number_input("กรอกส่วนสูงของคุณ (เซนติเมตร):", min_value=1.0)

# ส่วนที่ 3 สร้างปุ่มคำนวณ
if str.button("คำนวณค่า BMI 🎯"):
    # แปลงส่วนสูงจากหน่วย cm เป็น เมตร แล้วคำนวณ BMI
    height_m = height_cm / 100
    bmi = weight / (height_m ** 2)
    
    str.write("---")
    str.header(f"ค่า BMI ของคุณคือ: **{bmi:.2f}**")
    
    # ส่วนที่ 4 แปลผลค่า BMI ตามเกณฑ์
    if bmi < 18.5:
        str.warning("⚠️ คุณมีน้ำหนักน้อยกว่าเกณฑ์ (ผอม)")
    elif 18.5 <= bmi < 23.0:
        str.success("🟢 คุณมีน้ำหนักอยู่ในเกณฑ์ปกติ (สุขภาพดี)")
    elif 23.0 <= bmi < 25.0:
        str.info("🟡 คุณเริ่มมีน้ำหนักเกินเกณฑ์ (ท้วม)")
    else:
        str.error("🚨 คุณอยู่ในเกณฑ์อ้วน ควรระวังเรื่องสุขภาพและออกกำลังกาย")

# ส่วนแสดงข้อมูลผู้พัฒนาด้านล่างสุด
str.divider()
str.write("นายนันทิพัฒน์ ไพเชฐศักดิ์เลขที่ 2 ม.4/15")
